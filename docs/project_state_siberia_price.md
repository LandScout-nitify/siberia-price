# Project State — Siberia Price

**Проект:** `siberia-price`  
**Репозиторий:** `https://github.com/LandScout-nitify/siberia-price`  
**Дата фиксации:** 29.06.2026  
**Назначение файла:** сохранить логику развития проекта, текущий прогресс, принятые решения и ближайшие задачи, чтобы не потерять контекст при сжатии переписки.

---

## 1. Суть проекта

Проект `siberia-price` создается для исследования удельной кадастровой стоимости земельных участков промышленного назначения в Республике Тыва и регионах Сибири.

Главная управленческая гипотеза:

> Кадастровая стоимость земельных участков коммерческих и промышленных видов разрешенного использования в Республике Тыва может быть существенно завышена относительно сопоставимых индустриальных локаций других регионов Сибири. Это может создавать избыточную налоговую нагрузку, снижать инвестиционную привлекательность индустриального бизнеса и тормозить экономический рост региона.

Исследование не должно заранее доказывать завышение. Оно должно проверять гипотезу на большом массиве данных с нормализацией ВРИ, площади, локации, инфраструктурной доступности и рыночных ориентиров.

---

## 2. Управленческая цель

Подготовить независимое, воспроизводимое и доказательное сравнительное исследование для последующего разговора с:

- Главой Республики Тыва;
- Роскадастром;
- ГБУ, ответственным за государственную кадастровую оценку;
- иными органами, влияющими на подходы к кадастровой оценке и налоговой нагрузке на индустриальный бизнес.

Итоговая позиция должна строиться не на тезисе «в Туве всё завышено», а на проверяемой формуле:

> Если промышленные земли в Туве системно дороже сопоставимых локаций Сибири при более слабой экономической базе, слабой логистике и низкой рыночной ликвидности, то кадастровая стоимость становится скрытым налоговым барьером для индустриального развития региона.

---

## 3. Ключевая методологическая логика

Сравнивать нужно не «среднюю кадастровую стоимость по региону», а сопоставимые группы участков.

Основная единица анализа:

```text
один земельный участок
```

Главный расчетный показатель:

```text
КС 1 м² = кадастровая стоимость участка / площадь участка
```

Средневзвешенный показатель:

```text
Средневзвешенная КС 1 м² = сумма кадастровой стоимости участков / сумма площади участков
```

В итоговом исследовании должны использоваться одновременно:

- медианная КС 1 м²;
- средняя КС 1 м²;
- средневзвешенная КС 1 м²;
- P25 / P75;
- количество участков;
- суммарная площадь;
- доля выбросов;
- индекс Тувы к региону-бенчмарку;
- коэффициент кадастровой стоимости к рынку;
- налоговая нагрузка на типовые участки 1 / 3 / 10 / 20 га.

---

## 4. Регионы сравнения

Базовый регион:

- Республика Тыва.

Регионы-бенчмарки:

- Красноярский край;
- Томская область;
- Республика Хакасия;
- Республика Алтай;
- Иркутская область;
- Кемеровская область — Кузбасс;
- Алтайский край;
- Новосибирская область — опционально, осторожно, как сильный агломерационный рынок.

Ключевые локации Тувы:

- Кызыл;
- промышленные зоны Кызыла;
- пригородные индустриальные территории Кызылского кожууна;
- Ак-Довурак;
- Шагонар;
- Чадан;
- Туран.

---

## 5. Классы локаций

Для сопоставимости используются локационные классы:

| Тип | Описание |
|---|---|
| A | городская промзона |
| B | пригородная индустриальная зона 0–15 км |
| C | периферийная производственная территория 15–50 км |
| D | малый промышленный населенный пункт |
| E | изолированная территория |

Главный фокус будущего исследования — классы A и B.

---

## 6. Источники данных

Планируемые источники:

1. НСПД.
2. Росреестр / ЕГРН.
3. Фонд данных государственной кадастровой оценки.
4. Наши выгрузки и инструменты проекта анализа земельных участков.
5. Сделки ЕГРН.
6. Объявления о продаже промышленных земель.
7. Банкротные торги.
8. OpenStreetMap и открытые геоданные.
9. Официальная экономическая статистика регионов.

Важный принцип:

> Нельзя сразу строить итоговую аналитику на processed-таблицах без сохранения raw-слоя. Для доказательности нужно иметь трассировку: сырой ответ / исходная запись → нормализованная запись → расчет → итоговая таблица.

---

## 7. Техническая архитектура проекта

Репозиторий `LandScout-nitify/siberia-price`.

Основная структура:

```text
docs/                 методология и проектная документация
config/               регионы, ВРИ, правила очистки
data/                 examples, raw, interim, processed, external
src/siberia_price/    устанавливаемый Python-пакет
outputs/              таблицы, карты, графики, отчеты
app/                  будущий Streamlit-дашборд
tests/                тесты
```

Базовый стек:

- Python;
- pandas;
- geopandas;
- duckdb;
- pyarrow;
- osmnx;
- streamlit;
- plotly;
- rapidfuzz;
- pytest;
- pydantic;
- loguru.

Решение:

> Не подключать чужие репозитории как обязательную зависимость на старте. Репозитории вроде `rosreestr2coord` использовать как референс, но модуль НСПД писать свой, чтобы контролировать воспроизводимость и доказательность.

---

## 8. Завершенные этапы

### Этап 1. Методологический каркас

Создан корневой `.md` проекта с:

- целью;
- гипотезой;
- регионами-бенчмарками;
- источниками данных;
- структурой базы;
- нормализацией ВРИ;
- классификацией локаций;
- формулами;
- этапами проекта;
- вопросами к Роскадастру / ГБУ;
- журналом решений;
- следующими шагами.

Статус: завершено.

---

### Этап 2. Создание репозитория и стартового каркаса

Пользователь создал репозиторий:

```text
https://github.com/LandScout-nitify/siberia-price.git
```

Был подготовлен и загружен стартовый каркас проекта:

- `README.md`;
- `requirements.txt`;
- `.gitignore`;
- `.env.example`;
- `CODEX_PROMPT.md`;
- `docs/`;
- `config/`;
- `src/`;
- `data/`;
- `outputs/`;
- `app/`;
- `tests/`.

Статус: завершено.

---

### Этап 3. Проверка стартового каркаса Codex

Codex проверил и минимально доработал проект.

Выполнено:

- добавлены отсутствовавшие каталоги `data/` и `outputs/` с `.gitkeep`;
- обновлен `README.md`;
- обновлен `requirements.txt`;
- удалены необязательные тяжелые зависимости:
  - `pyrosm`;
  - `duckdb-engine`;
  - `folium`;
- добавлен `pytest`;
- добавлен `pyproject.toml`;
- добавлены `__init__.py` в подпакеты;
- обновлен `.gitignore`;
- проект успешно установлен в editable mode;
- `pip check` без ошибок;
- тесты: `2 passed`;
- НСПД не реализовывался.

Коммит Codex:

```text
1432268 Prepare installable project scaffold
```

Статус: завершено.

---

### Этап 4. Классификатор ВРИ

Codex доработал классификатор ВРИ.

Результат:

- 5 включаемых групп;
- 6 категорий исключений;
- 125 ключевых фраз;
- статусы:
  - `included`;
  - `excluded`;
  - `manual_review`;
- возвращаются:
  - группа;
  - метка;
  - ключевая фраза;
  - причина исключения;
- добавлены тесты промышленности, складов, ИЖС, ЛПХ, СНТ, сельхозземель, дорог, линейных объектов и спорных формулировок;
- документация обновлена;
- НСПД не изменен;
- проверка: `19 passed`, `pip check` без ошибок.

Ключевая методологическая логика:

1. Исключающие признаки имеют приоритет над включающими.
2. В основную выборку идут только `included`.
3. `manual_review` должны проверяться специалистом до итоговых расчетов.
4. Классификация детерминированная, по словарю `config/vri_classifier.yaml`.
5. Не используется черный ящик.

Статус: завершено.

---

### Этап 5. Пилотный пайплайн обработки таблицы

Codex реализовал пилотный пайплайн без подключения НСПД.

Результат:

- добавлен синтетический CSV:
  - `data/examples/industrial_land_sample.csv`;
  - 15 строк;
  - только синтетические идентификаторы;
- реализован:
  - `src/siberia_price/pipeline/process_land_table.py`;
- невалидные строки маркируются, но сохраняются;
- CLI создал Parquet:
  - 15 строк;
  - 13 валидных;
  - 2 невалидных;
- классификация:
  - 7 `included`;
  - 6 `excluded`;
  - 2 `manual_review`;
- добавлены тесты:
  - `tests/test_process_land_table.py`;
- результат:
  - `25 passed`;
- НСПД не изменен.

Ключевое решение:

> Невалидные строки не удаляются, а сохраняются с `data_quality_status=invalid` и причиной в `data_quality_reason`. Это важно для аудита и воспроизводимости.

Статус: завершено.

---

## 9. Текущая цепочка обработки

На текущий момент работает логика:

```text
синтетическая таблица
→ проверка качества числовых полей
→ классификация ВРИ
→ расчет кадастровой стоимости 1 м²
→ сохранение результата в Parquet
```

Пока не реализовано:

```text
НСПД API
массовый сбор данных
реальные кадастровые номера
геоаналитика расстояний
сопоставление с рынком
региональные метрики
дашборд
```

---

## 10. Текущие важные запреты

До отдельного решения не делать:

1. Не запускать массовый сбор НСПД.
2. Не использовать реальные кадастровые номера в тестовых данных.
3. Не строить вывод о завышении кадастровой стоимости до сбора и нормализации данных.
4. Не смешивать `included`, `excluded`, `manual_review` в итоговой аналитической выборке.
5. Не удалять невалидные строки без следа.
6. Не использовать NLP/ML как черный ящик для классификации ВРИ.
7. Не подключать тяжелую инфраструктуру вроде PostGIS до необходимости.
8. Не превращать проект в production-сервис раньше пилотного исследования.

---

## 11. Следующий рекомендуемый этап

Следующий этап:

> подготовить безопасный raw-слой и парсер синтетических ответов НСПД без сетевых запросов и без массового сбора.

Задача для Codex:

```text
Открой репозиторий LandScout-nitify/siberia-price.

Следующая задача: подготовить безопасный каркас работы с реальными ответами НСПД без массового сбора данных.

Нужно:

1. Создать папку:
   data/examples/nspd/

2. Добавить 2–3 синтетических JSON-примера ответа НСПД:
   - промышленный участок;
   - складской участок;
   - участок с неполными данными.

   Важно: данные должны быть синтетическими, без реальных кадастровых номеров.
   Использовать идентификаторы вида SYNTH-NSPD-001.

3. Доработать модуль:
   src/siberia_price/nspd/parser.py

   Он должен уметь извлекать из JSON:
   - cadastral_number;
   - area_m2;
   - cadastral_value;
   - land_category;
   - permitted_use_raw;
   - address;
   - geometry;
   - source = nspd.

4. Парсер должен быть устойчивым к разным вариантам имен полей:
   - cad_num / cn / cadastral_number;
   - declared_area / area / area_m2;
   - cost_value / cadastral_value;
   - readable_address / address;
   - permitted_use / readable_vri / permitted_use_established_by_document.

5. Добавить сохранение сырого JSON:
   src/siberia_price/io/raw_store.py

   Функции:
   - save_raw_json(payload, output_path)
   - load_raw_json(input_path)

6. Добавить тесты:
   - парсер корректно читает полный JSON;
   - парсер не падает на неполном JSON;
   - отсутствующие поля возвращаются как None;
   - сырой JSON сохраняется и читается без потерь;
   - реальные запросы в интернет не выполняются.

7. Обновить docs/data_sources.md:
   - добавить принцип: каждый исходный ответ НСПД сохраняется в raw-слое до нормализации;
   - итоговые расчеты должны быть воспроизводимы от raw JSON до processed Parquet.

8. Обновить docs/methodology.md:
   - добавить раздел "Raw-слой и трассируемость данных".

9. Не реализовывать сетевой клиент НСПД.
10. Не делать массовых запросов.
11. Все тесты должны проходить.

Цель: подготовить безопасный и проверяемый слой для будущей работы с НСПД, не начиная массовый сбор данных.
```

---

## 12. Следующие этапы после raw-слоя

### Этап 6. Пилот по реальным кадастровым номерам

Цель:

- взять 10–20 реальных участков Кызыла;
- вручную или полуавтоматически получить карточки;
- сохранить raw-ответы;
- прогнать через парсер;
- получить первую processed-таблицу.

Важно:

- не делать массовый сбор;
- проверить качество полей;
- сверить ВРИ;
- выявить проблемы формата НСПД.

---

### Этап 7. Пилотная аналитика Кызыла

Цель:

- рассчитать КС 1 м²;
- посчитать медиану, среднюю, средневзвешенную;
- выделить included/excluded/manual_review;
- проверить выбросы;
- сформировать таблицу проблемных участков.

---

### Этап 8. Сравнение Кызыл — Томск — Абакан — Красноярск

Цель:

- собрать малые пилотные выборки по сопоставимым промышленным локациям;
- проверить сопоставимость;
- рассчитать первые индексы;
- увидеть, есть ли предварительные признаки аномалии.

---

### Этап 9. Геоаналитика

Цель:

- рассчитать расстояния до центра города;
- до дорог;
- до железной дороги;
- до аэропорта;
- до промышленных зон;
- присвоить локационные классы A/B/C/D/E.

---

### Этап 10. Рыночная проверка

Цель:

- сопоставить кадастровую стоимость с:
  - сделками ЕГРН;
  - объявлениями;
  - банкротными торгами;
  - инвестиционными площадками;
- рассчитать коэффициент КС / рынок.

---

### Этап 11. Налоговая модель

Цель:

- рассчитать земельный налог для типовых участков:
  - 1 га;
  - 3 га;
  - 10 га;
  - 20 га;
- сравнить налоговую нагрузку по регионам;
- перевести кадастровую стоимость в понятный управленческий аргумент.

---

### Этап 12. Итоговый доклад

Цель:

- подготовить аналитический доклад;
- презентацию;
- записку для Главы Республики Тыва;
- перечень вопросов к Роскадастру / ГБУ;
- предложения по дальнейшим действиям.

---

## 13. Что нужно помнить при продолжении диалога

Если контекст будет сжат, восстановить логику проекта по этому файлу.

Короткая версия:

```text
Мы строим доказательное исследование по промышленным землям Сибири.
Не доказываем заранее завышение Тувы, а проверяем гипотезу.
Уже есть репозиторий siberia-price.
Каркас проекта готов.
Классификатор ВРИ готов.
Пилотный pipeline CSV → Parquet готов.
Тесты прошли: 25 passed.
Следующий шаг — raw-слой и parser НСПД на синтетических JSON без сетевых запросов.
Массовый сбор НСПД пока запрещен.
```

---

## 14. Формат дальнейшего ведения файла

После каждого этапа добавлять:

```text
### Этап N. Название

Дата:
Что сделано:
Какие файлы изменены:
Какие тесты прошли:
Какие решения приняты:
Какие риски обнаружены:
Следующий шаг:
Статус:
```

Этот файл нужно обновлять после каждого значимого шага проекта.
